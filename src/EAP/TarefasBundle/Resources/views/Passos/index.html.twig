{% extends 'TarefasBundle::base.html.twig' %}

{% block body -%}
    <h1>Passos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Data</th>
                <th>Obs</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('passos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.data %}{{ entity.data|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.obs }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('passos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('passos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('passos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
